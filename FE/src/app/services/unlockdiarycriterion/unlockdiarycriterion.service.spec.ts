import { TestBed } from '@angular/core/testing';

import { UnlockdiarycriterionService } from './unlockdiarycriterion.service';

describe('UnlockdiarycriterionService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: UnlockdiarycriterionService = TestBed.get(UnlockdiarycriterionService);
    expect(service).toBeTruthy();
  });
});
